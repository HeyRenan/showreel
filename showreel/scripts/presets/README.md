# Step presets for `node scripts/rec.mjs <url> out.gif --steps <preset>`
Presets target the rec >=0.9 step vocabulary (adds fill/select/camera); always check with `--dry` first.
Selectors are EXAMPLES from the bundled demo page (assets-src/demo/index.html) — swap them for your app.
